package io.github.vesas.jdbcprof.analysis;

/**
 * CLI entry point for the offline analysis stage (spec §8).
 *
 * <p>Phase 2 replaces this stub with a picocli-driven command that
 * reads a {@code .jdbclog} file and emits the HTML report.
 */
public final class AnalyzeCli {

    private AnalyzeCli() {
    }

    public static void main(String[] args) {
        System.err.println("jdbc-profile analyze: not implemented (phase 2)");
        System.exit(2);
    }
}
